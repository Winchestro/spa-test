To run install nodejs and just run "node server".

It's a very minimalistic single page app with no additional dependencies, uses only standard node modules. Create profiles with avatars and write posts. Uses basic JWT authentification.
